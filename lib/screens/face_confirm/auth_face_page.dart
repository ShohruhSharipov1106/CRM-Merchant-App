import 'package:crm_merchant/constants/exports.dart';

class AuthFacePage extends StatelessWidget {
  const AuthFacePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}